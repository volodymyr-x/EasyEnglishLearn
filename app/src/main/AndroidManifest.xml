<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vladimir.easyenglishlearn" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="PreferencesActivity"></activity>
        <activity android:name=".CategoryActivity"></activity>
        <activity android:name=".EditCollectionActivity" ></activity>
        <activity android:name=".NewCollectionActivity" ></activity>
        <activity android:name=".WordSelection" ></activity>
        <activity android:name=".WordQuiz" ></activity>
        <activity android:name=".WordConstructor" ></activity>
    </application>

</manifest>
