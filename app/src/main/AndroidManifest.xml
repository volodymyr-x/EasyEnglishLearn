<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vladimir.easyenglishlearn" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".preferences.PreferencesActivity"></activity>
        <activity android:name=".CategoryActivity"></activity>
        <activity android:name=".EditCategoryActivity" ></activity>
        <activity android:name=".NewCategoryActivity" ></activity>
        <activity android:name=".WordSelectionActivity" ></activity>
        <activity android:name=".WordQuizActivity" ></activity>
        <activity android:name=".WordConstructorActivity" ></activity>
    </application>

</manifest>
